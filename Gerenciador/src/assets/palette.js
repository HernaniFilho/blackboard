import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    custom: {
      white: "#FFFFFF",
      beige: "#F5EFEB",
      skyBlue: "#C8D9E6",
      teal: "#567C8D",
      navy: "#2F4156",
      green: "#8bc34a",
      red: "#ff1744",
    },
  },
});

export default theme;
